baseURL = "http://localhost:1313/"
languageCode = "en-us"
title = "Alireza Sohofi"
# theme = ""  <-- IMPORTANT: Commented out or deleted

# Keep other settings like taxonomies etc. if you added them
[taxonomies]
  tag = "tags"

[outputs]
  home = ["HTML", "RSS"]
  page = ["HTML"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  term = ["HTML", "RSS"]

[params]
  linkedinUrl = "https://linkedin.com/in/alireza-sohofi"

[build]
  writeStats = true 

[markup.goldmark.renderer]
  unsafe = true